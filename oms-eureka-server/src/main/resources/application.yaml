server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # the registry doesn't register with itself
    fetch-registry: false         # ...and doesn't need to fetch a registry
  instance:
    hostname: localhost